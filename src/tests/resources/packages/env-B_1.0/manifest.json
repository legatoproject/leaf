{
  "info": {
    "name": "env-B",
    "version": "1.0"
  },
  "env": {
    "LEAF_ENV_B": "BAR",
    "LEAF_PATH_B": "$PATH:@{DIR}"
  },
  "env-activate": [
    "@{DIR}/env.in",
    "@{DIR}/otherenv.in"
  ],
  "env-deactivate": [
    "@{DIR}/env.out",
    "@{DIR}/otherenv.out"
  ]
}